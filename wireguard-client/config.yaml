name: "WireGuard Client Dev"
description: "Connect your Home Assistant instance to a Wireguard VPN."
version: "1.0.1.2"
slug: "wg-client"
init: false
arch:
  - amd64
  - aarch64
# image: "ghcr.io/vegardbeider/wireguard-client-{arch}-addon" 
map: 
  - addon_config:rw
privileged:
  - NET_ADMIN
host_network: true
startup: once
device: /dev/net/tun